import THEMES from "@/assets/data/themes.json";

export default Object.freeze({
  THEMES: THEMES,
  IGNORED_THEMES: ["them.TEC", "them.OTH"],
  CSV_THEME_KEYS: ["Theme.FINDINGS", "Theme.IMPLICATIONS"],
  ARTICLE_LINK_KEY: ["New Links"],
});
